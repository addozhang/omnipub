# Security
SECRET_KEY=change-me-in-production-use-a-long-random-string

# Database (default: SQLite, change to PostgreSQL for production)
# DATABASE_URL=sqlite+aiosqlite:///./data/multi_publisher.db
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/multi_publisher

# App settings
DEBUG=false

# Frontend port (docker-compose)
PORT=3000
