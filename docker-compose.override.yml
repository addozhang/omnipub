# docker-compose.override.yml — 本地开发用，覆盖默认配置
# 自动被 docker compose 读取，无需 -f 参数
services:
  backend:
    image: ""
    build:
      context: .
      dockerfile: Dockerfile

  frontend:
    image: ""
    build:
      context: ./frontend
      dockerfile: Dockerfile
