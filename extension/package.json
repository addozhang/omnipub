{
  "name": "omnipub-extension",
  "version": "1.4.0",
  "description": "Omnipub Chrome Extension",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack": "bash ../pack-extension.sh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.1",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "vitest": "^3.2.1"
  }
}
